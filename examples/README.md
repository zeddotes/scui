# Examples of SCUI Usage

*Note:* Because examples are part of the same repository as the source for SCUI, `tsconfig.json` points to local `paths` for dev purposes. Remove these paths and install SCUI dependencies via your favourite package manager to get a functioning example. 